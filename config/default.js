const config = {
    // 启动端口
    port: 300,

    // 数据库配置
    database: {
        DATABASE: 'cake',
        USERNAME: 'root',
        PASSWORD: '',
        PORT: '3306',
        HOST: 'localhost'
    }
}

module.exports = config